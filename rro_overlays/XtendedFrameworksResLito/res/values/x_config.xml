<?xml version="1.0" encoding="utf-8"?>
<!--
     Copyright (C) 2015-2016 The CyanogenMod Project
                   2017-2021 The LineageOS Project
     SPDX-License-Identifier: Apache-2.0
-->
<resources>
    <!-- Default value for proximity check on screen wake
     NOTE ! - Enable for devices that have a fast response proximity sensor (ideally < 300ms)-->
    <bool name="config_proximityCheckOnWake">true</bool>
    <bool name="config_proximityCheckOnWakeEnabledByDefault">true</bool>

    <!-- Hardware keys present on the device, stored as a bit field.
         This integer should equal the sum of the corresponding value for each
         of the following keys present:
             1 - Home
             2 - Back
             4 - Menu
             8 - Assistant (search)
            16 - App switch
            32 - Camera
            64 - Volume rocker
         For example, a device with Home, Back and Menu keys would set this
         config to 7. -->
    <integer name="config_deviceHardwareKeys">64</integer>

    <!-- Hardware keys present on the device with the ability to wake, stored as a bit field.
         This integer should equal the sum of the corresponding value for each
         of the following keys present:
             1 - Home
             2 - Back
             4 - Menu
             8 - Assistant (search)
            16 - App switch
            32 - Camera
            64 - Volume rocker
         For example, a device with Home, Back and Menu keys would set this
         config to 7. -->
    <integer name="config_deviceHardwareWakeKeys">64</integer>

    <!-- Whether device has screen with higher aspect ratio -->
    <bool name="config_haveHigherAspectRatioScreen">true</bool>

    <!-- Defines the actions shown in advanced reboot submenu -->
    <string-array name="config_restartActionsList">
        <item>restart</item>
        <item>restart_recovery</item>
        <item>restart_bootloader</item>
        <item>restart_fastboot</item>
    </string-array>

    <!-- Whether the device supports wireless charging or not -->
    <bool name="config_deviceSupportsWirelessCharging">false</bool>

    <!-- The list of package IDs that are allowed to skip camera high frame rate checks. -->
    <string-array name="config_cameraHFRPrivAppList" translatable="false">
        <item>com.android.camera</item>
        <item>com.oplus.camera</item>
        <item>org.lineageos.aperture</item>
    </string-array>

    <!-- Whether device has physical tri state switch -->
    <bool name="config_hasAlertSlider">false</bool>

    <!-- Whether the device supports Smart Pixels -->
    <bool name="config_supportSmartPixels">true</bool>

    <!-- Whether devices suports in-display fingerprint when screen is off -->
    <bool name="config_supportScreenOffUdfps">true</bool>
    <bool name="config_udfps_sensor_props">true</bool>
    <bool name="config_supportsUdfps">true</bool>

    <!-- Xtensions Settings -->
    <!-- Screen Off FOD -->
    <bool name="screen_off_udfps_enabled">true</bool>

    <!-- Network traffic -->
    <bool name="network_traffic_state">true</bool>
    <!-- Auto-hide -->
    <integer name="network_traffic_autohide_threshold">0</integer>
    <!-- Show arrows -->
    <bool name="network_traffic_arrow">true</bool>

    <!-- Swipe to screenshot -->
    <bool name="three_finger_gesture">true</bool>

    <!-- Double tap on status bar to sleep -->
    <bool name="double_tap_sleep_gesture">false</bool>

    <!-- Settings dashboard style -->
    <bool name="custom_ui_toggle">true</bool>

    <!-- Ignore windows secure flags -->
    <bool name="window_ignore_secure">true</bool>

    <!-- Status bar padding -->
    <integer name="statusbar_left_padding">20</integer>
    <integer name="statusbar_right_padding">7</integer>

    <!-- AM/PM styles -->
    <integer name="status_bar_am_pm_values">0</integer>
</resources>
